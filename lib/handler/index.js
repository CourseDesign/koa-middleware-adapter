const extractParameterHandler = require('./extract-parameter-handler');
const injectResponseHandler = require('./inject-response--handler');
const errorHandler = require('./error-handler');

module.exports = {
  extractParameterHandler,
  injectResponseHandler,
  errorHandler,
};
