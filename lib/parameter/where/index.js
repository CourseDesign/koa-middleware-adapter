const Where = require('./where');

const headers = require('./headers');
const params = require('./params');
const query = require('./query');
const body = require('./body');
const cookies = require('./cookies');

module.exports = {
  Where, headers, params, query, body, cookies,
};
